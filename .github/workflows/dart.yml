name: Dart

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dart-lang/setup-dart@v1.3
        with:
          sdk: 2.19.5
#      - name: Flutter install and get dependencies
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - run: flutter pub get
#      - run: flutter test
      - run: flutter --version
      - name: dart add test in dev_dependencies
        run: dart pub add --dev test
      - name: Install Dart dependencies
        run: dart pub get
      - name: Run tests
        run: flutter analyze
